#ifndef _GAUSSFRACTION_H_
#define _GAUSSFRACTION_H_

#include "fraction.h"
#include <vector>

std::vector<Fraction<long> > gaussFraction(std::vector<std::vector<Fraction<long> > >, std::vector<Fraction<long> >);

#endif