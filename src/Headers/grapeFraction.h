#ifndef _GRAPEFRACTION_H_
#define _GRAPEFRACTION_H_

#include "fraction.h"
#include <vector>

std::vector<std::vector<Fraction<long> > > grapeFraction(const std::vector<std::vector<Fraction<long> > >& , const std::vector<std::vector<Fraction<long> > >&);

#endif